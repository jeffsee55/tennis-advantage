before_install:
  - "echo '--colour' > ~/.rspec"
  - "echo 'gem: --no-document' > ~/.gemrc"
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
before_script:
  - cp .sample.env .env

  - psql -c 'create database "gregsons_tennis_advantage_test";' -U postgres

branches:
  only:
    - master
cache:
  - bundler
language:
  - ruby
notifications:
  email:
    - false
rbenv:
  - 2.1.2
addons:
  postgresql: "9.3"
